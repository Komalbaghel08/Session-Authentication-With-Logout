This ViewSet uses Django REST Framework with Session Authentication and IsAuthenticated permission. 
Only authenticated users can access the Student data. 
The ModelViewSet provides default CRUD operations (create, read, update, delete) for the Student model.
